<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_theme1</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_theme1</item>
        <item name="colorAccent">@color/colorAccent_theme1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme2" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_theme1</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_theme1</item>
        <item name="colorAccent">@color/colorAccent_theme1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme3" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary_theme1</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_theme1</item>
        <item name="colorAccent">@color/colorAccent_theme1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/dialog_button_color</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:background">#fbfbfb</item>
        <item name="android:windowTitleStyle">@style/MyTitleTextStyle1</item>
    </style>

    <style name="MyTitleTextStyle1">
        <item name="android:textColor">@color/dialog_button_color</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <style name="SwitchButtonStyle">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">0dp</item>
    </style>

    <!-- You can also inherit from NNF_BaseTheme.Light -->
    <style name="FilePickerTheme" parent="NNF_BaseTheme.Light">
        <!-- Set these to match your theme -->
        <item name="colorPrimary">@color/color_file_manager_toolbar</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_theme1</item>
        <item name="colorAccent">@color/color_file_manager_dir</item>

        <!-- Setting a divider is entirely optional -->
        <item name="nnf_list_item_divider">?android:attr/listDivider</item>

        <!-- Need to set this also to style create folder dialog -->
        <item name="alertDialogTheme">@style/FilePickerAlertDialogTheme</item>

        <!-- If you want to set a specific toolbar theme, do it here -->
         <item name="nnf_toolbarTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>

    <style name="FilePickerAlertDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorPrimary">@color/color_file_manager_toolbar</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_theme1</item>
        <item name="colorAccent">@color/color_file_manager_dir</item>
    </style>


    <!--Navigation view style-->

    <style name="Theme_check_box" parent="AppTheme" >
        <item name="colorAccent">@color/colorAccent_cb</item>
        <item name="android:textColorSecondary">@color/colorAccent_cb</item>
    </style>

    <!--Navigation view style-->
    <style name="NavigationView"/>

    <style name="NavigationView.Item" parent="NavigationView"/>

    <style name="NavigationView.Item.Layout" parent="NavigationView.Item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="NavigationView.Item.Button" parent="NavigationView.Item.Layout">
        <item name="android:clickable">true</item>
    </style>

</resources>
